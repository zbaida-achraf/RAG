# RAG
This repository contains a pipeline that allows you to upload PDFs and ask questions, which are then answered by a Large Language Model (LLM). It's based on a Retrieval-Augmented Generation (RAG) approach.
